{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Character",
  "description": "A character in the narrative — voice, traits, constraints",
  "type": "object",
  "required": ["character_id", "name"],
  "properties": {
    "character_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_]+$",
      "description": "Unique identifier used in scenes, e.g. D, J"
    },
    "name": {
      "type": "string",
      "description": "Display name"
    },
    "voice_notes": {
      "type": "string",
      "description": "Brief notes on speech patterns, vocabulary, rhythm"
    },
    "traits": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Consistent traits that inform dialogue"
    },
    "forbidden_expressions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Phrases this character would never say"
    }
  }
}
